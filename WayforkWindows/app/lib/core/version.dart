/// The app version, kept in step with `pubspec.yaml` (the MSI and the
/// service report their own; a difference is logged at connect time).
abstract final class WayforkVersion {
  static const app = '0.3.0';
}
