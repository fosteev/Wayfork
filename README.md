# Wayfork
macOS menu bar app for per-domain split tunneling across multiple VPNs at once — OpenVPN and VLESS (via sing-box). Route specific sites through specific tunnels, everything else goes direct.
