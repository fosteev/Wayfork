# UX

Menu bar app (`LSUIElement`), no Dock icon. Visual reference: approved
[prototype/variant-b.html](prototype/variant-b.html) (popover, sidebar Settings, grouped rules)
as redrawn by [prototype/variant-c.html](prototype/variant-c.html) (approved 2026-09-15 for
the F14–F18 wave — see "Variant C" at the end of this file, which amends the sections
below screen by screen);
[prototype/index.html](prototype/index.html) is the rejected v1 kept for the Logs window,
helper alert and Add VLESS sheet, which are unchanged.

Surfaces: the menu bar **popover**, the **Settings** window, the **Logs** window, plus a few
sheets and alerts.

## Menu bar icon

Custom template asset (fork-in-the-road glyph), four variants:

| Global state | Icon | Notes |
|--------------|------|-------|
| off          | outline | |
| starting / stopping | filled, `.symbolEffect(.pulse)` | |
| on           | filled | |
| degraded     | filled, one branch hollow | tooltip (`.help`) carries the summary line, which names the failing tunnel |
| error        | outline + exclamation | |

Click opens the popover (`MenuBarExtra` with `.window` style: a borderless window anchored
under the icon, closes on click outside or Esc).

## Popover

320 pt wide, vertical stack, follows system appearance (light/dark):

```
[icon] Wayfork                                   [toggle]
       On — routing 6 domains through 2 tunnels
────────────────────────────────────────────────
┌ ● Work      OpenVPN   ↓ 1.2 MB/s ↑ 85 KB/s [↻] ┐
│   connected · 10.8.0.6 on utun101 · 3 rules   │
└───────────────────────────────────────────────┘
┌ ● Home      VLESS      ↓ 0 B/s ↑ 0 B/s        ┐
│   ready · host.example.com · 2 rules          │
└───────────────────────────────────────────────┘
┌ ◐ Lab                        OpenVPN      [↻] ┐
│   connecting… attempt 2 · tls-error           │
└───────────────────────────────────────────────┘
   Direct                  ↓ 200 KB/s ↑ 12 KB/s
────────────────────────────────────────────────
[ Route domain…            ] [Work ▾]  [Add]
────────────────────────────────────────────────
Settings ⌘,    Logs ⌘L                  Quit ⌘Q
```

- **Header**: glyph, name, global toggle. Toggle is disabled while starting/stopping (icon
  pulses). Summary line by state:
  - off: "Off — all traffic goes direct"
  - starting: "Starting…"
  - on: "On — routing N domains through M tunnels" ("On — no tunnels" when none enabled);
    with a default tunnel (F8): "On — everything via <tunnel> · N rules, M exceptions"
  - degraded: "Degraded — <tunnel> is failing · N domains, M tunnels up"; when the failing
    tunnel is the default: "Degraded — <tunnel> (default) is down · unmatched traffic is
    blocked"
  - error: "Routing engine failed — see Logs"
- **Tunnel cards**, one per *enabled* tunnel in store order (changed 2026-08-25: disabled
  tunnels are not listed — they live in Settings › Tunnels, which shows everything; when
  every tunnel is disabled the list is replaced by "All tunnels are disabled." with a
  "Manage tunnels…" button). Line 1: status glyph, name, type badge, action button.
  Line 2 (secondary, 11 pt): state details.
  - connected: `connected · <ip> on <utun> · N rules`, action ↻ (reconnect); the default
    tunnel (F8) appends `· everything else`
  - ready (VLESS): `ready · <host> · N rules`, no action
  - connecting/reconnecting: `connecting… attempt N · <reason>`, action ↻
  - failed: reason in red (`failed: server rejected username/password`), action ✎ (opens
    Settings › Tunnels with that tunnel expanded and the offending field focused)
  - global off: every card dimmed, `not running · N rules`, no actions
  Clicking a card body opens Settings › Tunnels with that tunnel expanded.
- **Traffic rates (F9)**, while the global state is on/degraded. Line 1 of a connected or
  ready card gets `↓ <rate> ↑ <rate>` between the type badge and the action button
  (11 pt, secondary, monospaced digits; dimmed to tertiary when both are 0). Not shown for
  connecting / reconnecting / failed cards or while off/starting/stopping. After the cards a
  slim **Direct** row (no card background, no action, not clickable) shows the rate of
  traffic that bypasses the tunnels; with a default tunnel (F8) that is exceptions and
  local names only, and the row is labelled `Direct · exceptions`. Hovering a rate shows
  `Since Turn On: ↓ 1.2 GB ↑ 88 MB · 14 connections`. Rate formatting: `ByteCountFormatter`
  decimal units with at most three significant digits plus `/s` (`0 B/s`, `85 KB/s`,
  `1.2 MB/s`, `12 MB/s`); totals the same without `/s`. Figures update once a second; when
  no sample has arrived for 3 s (daemon busy, Clash API down) the labels show `—` instead
  of stale numbers. The header summary is unchanged — each exit (every tunnel and Direct)
  carries its own figure exactly once, so a total is not repeated.
- Implementation notes (2026-08-25, F9): the strings come from `TrafficFormat`
  (`WayforkCore`), hand-rolled rather than `ByteCountFormatter` so tests and layout are
  locale-independent: one decimal below 10 (`1.0 KB`, `9.9 MB`), none above (`12 MB`,
  `999 KB`), `999.5` rolls over to the next unit, units stop at PB. The rate is right-aligned
  before the action button (the name truncates first). The Direct row appears only while
  on/degraded; the placeholder `↓ — ↑ —` carries the tooltip "No traffic sample in the last
  3 seconds". Staleness is a 3 s timer in `AppModel` re-armed by every sample; Off, a helper
  interruption and an engine that is not running clear the figures at once. A sample that
  arrives while the app is not `desiredOn` is ignored.
- **Dead-UDP hint (H3, 2026-09-01)**: when a tunnel's counters report one-way UDP flows
  (`oneWayUDPFlows > 0` — sent for ≥ 10 s, received nothing; docs/design/05-daemon.md,
  "Traffic sampling"), an orange ⚠ appears between the rate and the action button on that
  card, tooltip "N UDP connections sent data but received nothing for 10 s — the server may
  be dropping UDP" (`TrafficFormat.oneWayUDPHint`). Shown only where the rate itself is
  shown; the Direct row never carries it (a one-way UDP flow to the open internet says
  nothing about a tunnel server). The same counts land in the diagnostics `system.txt`.
- **Quick add**: domain field (prefilled from the clipboard when it looks like a URL/host),
  tunnel popup (last used; last item **Direct** adds an exception, F8), Add. Match type is `suffix`; a `*` in the input switches to
  `wildcard`, an IPv4 address or subnet to `ip` (F11). Invalid input → red outline + one-line error under the field. Existing pattern
  → button reads **Update**. Applies immediately (hot reload). Hidden while global off.
- **Footer**: Settings, Logs, Quit as text buttons with shortcuts.
- Empty store: the cards are replaced by "No tunnels yet" with an "Add a tunnel…" button
  that opens Settings › Tunnels; quick add is hidden until a tunnel exists.
- Status glyphs: green filled (connected/ready), grey hollow (disabled/not running), orange
  half (connecting/reconnecting), red cross (failed).

## Settings window

Single window "Wayfork Settings", 820×560, resizable. Left sidebar (180 pt) with
**Tunnels · Rules · General**; content area with a large section title.

### Tunnels

```
Tunnels                                                  [+ Add ▾]
┌──────────────────────────────────────────────────────────────────┐
│ ● Work   OpenVPN  connected · vpn.example.com:1194 udp …  [●] ▸  │
│    Name       [ Work            ]                               │
│    Username   [ alice           ]                               │
│    Password   [ •••••••••       ]                               │
│    DNS        (•) Automatic (10.8.0.1)  ( ) Custom [        ]   │
│    Config     Aug 25, 2026 · ab12…9f3e   [Replace…]             │
│               3 rules · Show          [Reconnect]  [Delete…]    │
├──────────────────────────────────────────────────────────────────┤
│ ● Home   VLESS    ready · host.example.com:443 · REALITY   [●] ▸ │
├──────────────────────────────────────────────────────────────────┤
│ ✕ Lab    OpenVPN  failed: server rejected username/password [●] ▸│
├──────────────────────────────────────────────────────────────────┤
│ ○ Test   VLESS    disabled · host2.example.com:443        [○] ▸  │
└──────────────────────────────────────────────────────────────────┘
```

- Rows: status glyph, name, type badge, one-line summary, enabled toggle, chevron. Clicking
  a row expands it in place (one expanded at a time); the toggle works without expanding.
- Expanded OpenVPN: Name, Username/Password (only when `needsCredentials`), Key passphrase
  (only when `needsKeyPassphrase`), DNS (Automatic with the discovered address / Custom),
  Config (import date, short hash, Replace…), footer with rule count link, Reconnect,
  Delete….
- Expanded VLESS: Name, URL (UUID masked, Copy, Replace URL…), footer with rule count link
  and Delete….
- Both kinds (F8): a toggle **Route everything else through this tunnel** above the footer.
  Turning it on moves the default from whichever tunnel had it (only one); the row summary
  gets `· everything else`. Hint under the toggle: "Domains without a rule use this tunnel;
  add exceptions in Rules › Direct. While it is down, unmatched traffic is blocked." If the
  default tunnel is disabled the hint turns orange: "Disabled — everything else goes direct."
- **+ Add** menu (F13): *Import OpenVPN Config…* (file picker), *Add WireGuard…* (file
  picker or paste), *Add from link…* (sheet, any supported scheme). The old
  *Add VLESS from URL…* entry is gone — one sheet now takes `vless://`, `ss://`,
  `trojan://` and `vmess://` and tells them apart itself, so a user pasting a link never
  has to know which menu item matches it. Dropping a `.ovpn` or `.conf` file anywhere on
  the window imports it.
- Edits save on focus loss / toggle. While running: credentials/config change → reconnect
  that tunnel; enable/disable → routing restart; name → nothing.
- Delete with rules attached: "Delete tunnel and its N rules?" — rules go with it.

**Add from link sheet** (generalizes the Add VLESS sheet): text field prefilled from the
clipboard when it starts with a supported scheme; the scheme picks the parser; live parse
preview (Kind, Name, Server, and what matters for that kind — Security/Transport for
VLESS, Trojan and VMess, Method for Shadowsocks); Add enabled only when valid;
parse/unsupported errors inline in red, naming the kind ("Not a valid ss:// link:
unsupported method aes-256-cfb"). Same sheet in *Replace link…* mode from an expanded row,
where the kind is fixed and a link of another kind is refused.

The same field takes a **subscription URL** (F13 stage 7). When the text starts with
`https://` the preview area shows a *Fetch* button instead of the parse preview (Enter
triggers it; nothing is requested while typing); `http://` gets the inline error
"Subscriptions must use https". While fetching: a spinner and the host name, Cancel
still works. On success the sheet grows into a checklist:

```
Add Tunnels from Subscription                  papervpn.example  ·  3 servers, 1 skipped
[✓] NL            VLESS   <host>:443   REALITY
[✓] DE            Trojan  <host>:443   TLS
[ ] NL            VLESS   <host>:443   REALITY      already added
    line 4        hysteria2://…                     unsupported scheme hysteria2://
                                          2 of 12 free slots      [Cancel] [Add 2 tunnels]
```

Parsed servers are pre-checked, a server equal to an existing tunnel is unchecked and
labelled *already added*, skipped lines are listed greyed with the reason and cannot be
checked. The Add button counts the checked rows and is disabled at zero or when the count
exceeds the free slots (the footer then reads "only N slots free"). Fetch errors (status,
timeout, not a list of links) appear in the same red inline text as a bad link. The URL is
not kept anywhere after the sheet closes — no refresh, no history. In *Replace link…*
mode a URL is refused: "Paste a single link; subscriptions add new tunnels."

**Add WireGuard sheet**: *Choose File…* or a paste area for the conf body; the same live
preview (Name from the file name, Address, Peer endpoint, DNS, MTU); the `AllowedIPs`
warning appears here first, as a yellow note under the preview, and the tunnel can still be
added. Wayfork never writes the conf back out, so an unknown wg-quick key is listed in the
preview as ignored rather than blocking the import.

**Per-kind expanded rows and card summaries**:

| Kind | Row summary | Expanded fields |
|------|-------------|-----------------|
| WireGuard | `ready · <peer host>:<port> · WireGuard` | Name, Peer (host:port, public key first 8 chars), Address, DNS (Automatic from the conf / Custom), MTU, `AllowedIPs` badge when narrower than `0.0.0.0/0`, Replace config… |
| Shadowsocks | `ready · <host>:<port> · <method>` | Name, link (password masked, Copy, Replace link…) |
| Trojan | `ready · <host>:<port> · TLS` / `· REALITY` | Name, link (password masked, Copy, Replace link…) |
| VMess | `ready · <host>:<port> · <transport>` | Name, link (uuid masked, Copy, Replace link…) |

**Copy** reconstructs the full link from meta plus the Keychain secret for every link kind,
the same way VLESS already does; WireGuard has no link form, so its row offers no Copy —
the conf came from a file and the private key stays where it is. The `allowInsecure` badge
applies to Trojan and VMess exactly as it does to VLESS; the new `AllowedIPs` badge reads
"Routes only <prefix> — traffic sent elsewhere through this tunnel is dropped by the peer".

### Rules

```
Rules                                        [🔍 Search rules     ]
┌ ● Work   OpenVPN   4 rules                              [+▾] ┐
│ [✓] example.com                Suffix                    ⋮⋮ │
│ [✓] ▣ Telegram                 App                       ⋮⋮ │
│ [✓] api.internal.example.com   Exact                     ⋮⋮ │
│ [ ] old.example.com            Exact     paused          ⋮⋮ │
└─────────────────────────────────────────────────────────────┘
┌ ● Home   VLESS     2 rules                              [+] ┐
│ [✓] *.cdn.example.com          Wildcard                  ⋮⋮ │
│ [✓] news.example.org           Suffix                    ⋮⋮ │
└─────────────────────────────────────────────────────────────┘
┌ ○ Test   VLESS     1 rule                               [+] ┐   (dimmed)
│ [✓] docs.example.net           Suffix   tunnel disabled — goes direct │
└─────────────────────────────────────────────────────────────┘
```

- One group per tunnel, in store order; groups of disabled tunnels are dimmed.
- F8: a **Direct** group sits above the tunnel groups (grey glyph, badge "exceptions"). Its
  header hint reads "Everything else goes through <tunnel>; these domains stay direct" when
  a default tunnel is set, otherwise "Overrides tunnel rules; everything unmatched already
  goes direct". Rows, editing, drag & drop and search work like in tunnel groups; dragging a
  rule into it turns it into an exception. Local names (`.local`, `.lan`, `.internal`,
  `.home.arpa`) are always direct and listed as a non-editable footnote.
- Row: enabled checkbox, pattern (monospace, inline-editable), match popup, note, drag
  grip. `+` in the group header adds an empty row in edit mode; Enter commits, Esc
  discards; Delete key removes the selected row.
- Drag within a group reorders; drag to another group moves the rule to that tunnel.
- A pattern that also appears in an earlier group gets a **shadowed** chip with a tooltip
  ("`example.com` is already routed via Work", or "… is an exception" when the Direct
  group has it); a pattern covering a tunnel's own server
  host gets a **warning** chip.
- Search filters rows across groups; empty groups collapse to their header.
- Empty state: "No rules yet. Everything goes direct. Add a rule here or from the menu bar."
- Changes apply live; a rejected change shows an inline error under the group.
- F10: the `+` in a group header is a menu — **Domain** (empty row in edit mode, as before)
  or **Application…** (open panel limited to application bundles, starting in
  `/Applications`). An application row shows the app icon and display name instead of a
  pattern, the bundle path as tooltip and a fixed "App" label instead of the match popup;
  checkbox, note, drag & drop and the shadowed chip work as for domains. A bundle that no
  longer exists gets a **not found** chip ("`/Applications/Foo.app` is missing; the rule
  matches again once it is back"). Search matches app names and paths. Quick add in the
  menu bar has no application entry.
- F11: the pattern field also takes an IPv4 address or subnet (`10.8.0.0/24`); the match
  popup gains **IP** and switches to it on its own while typing, the placeholder reads
  `example.com or 10.0.0.0/24`, search matches addresses, and quick add accepts IPs. A
  rule that overlaps one of the Mac's own networks gets a **warning** chip ("covers your
  LAN (en0, 192.168.1.0/24); devices in it go through the tunnel while Wayfork is on").
  No file dialog and no extra menu item: an IP is typed where a domain is.
- Implementation notes (2026-08-25, F10): app rows have no inline edit — the context menu
  offers **Reveal in Finder** instead of Edit (delete and re-add to change the app); the
  match popup lists only the domain kinds (`RuleMatch.domainCases`). The `.app` suffix is
  checked case-insensitively, the stored path keeps its case. A path typed into the domain
  field fails as "Enter a domain" (URL-part stripping leaves nothing) — app rules come
  only through the dialog. Icon / display name / existence are cached per path on the main
  actor (`AppBundleInfo`).
- Fake IPs (2026-08-26): an address in `198.18.0.0/15` seen in the logs is one sing-box
  handed to a *name* and routes by that name, so a rule on it could never match. Pasting
  one into any pattern field (Rules, inline editor, quick add) replaces it on the spot
  with the wildcard of the name's domain — `198.18.0.118` that was issued to
  `gitlab.example.com` becomes `*.example.com` (a two-label name stays as a suffix rule, a
  name right under `co.uk`-style suffixes stays exact); the app log notes the swap. The
  names come from sing-box's `dns: exchanged A … IN A 198.18.x.x` lines (`FakeIPIndex`,
  rebuilt from the log tail at launch, so nothing is known at `warn` log level); a fake IP
  the logs have not explained fails with "… is a fake IP Wayfork issued to a name it has
  not logged yet — add the domain instead".
- Implementation notes (2026-08-25, F11): while typing, `*` switches the popup to Wildcard
  and an address or subnet to IP; editing an IP back into a name returns to Suffix, Exact
  and Wildcard are otherwise left as chosen. The placeholder is `example.com or
  10.0.0.0/24` in both the new row and the inline editor; the popover field reads "Route
  domain or IP…". An IP typed under a domain kind fails with "This is an IP address — pick
  the IP match". The LAN chip is the generic **warning** chip with the interface and network
  in its tooltip; `AppModel` refreshes the interface list at most every 10 s.

### General

Grouped forms: Startup (Launch at login, Connect on launch), Reliability (Reconnect
automatically, Notify when a tunnel fails), DNS (Direct traffic resolver: System / Custom),
Logs (Level, Keep logs for N days, Open Logs Folder), Helper & About (helper status with
Reinstall / Open System Settings… / Install Helper depending on state; version line;
Export Diagnostics…), Backup (Export… / Import… of `wayfork-export.json`, see
[01-data-model.md](01-data-model.md); the sheets described there open from here).

## Logs window

Separate window (⌘L), 900×500. Toolbar: source filter (All / App / Daemon / sing-box /
each tunnel), level filter (Error / Warning / Info / Debug), search field, Follow toggle,
Clear, Copy. Monospaced list, newest at the bottom, auto-scroll while Follow is on. Lines:
`HH:mm:ss.SSS  source  LEVEL  message`. Detail in [06-logging.md](06-logging.md).
Implementation note: Follow is a manual toggle in MVP (macOS 14 SwiftUI has no reliable
scroll-position hook; auto-off on scroll-up is deferred).

## First run and helper approval

First **Turn On**:

1. App calls `SMAppService.daemon(...).register()`.
2. Status `.requiresApproval` → alert:
   > **Allow Wayfork to run in the background.** Wayfork needs a helper with system
   > privileges to create the tunnel interface. Open System Settings → Login Items and
   > enable Wayfork under "Allow in the Background". Wayfork continues automatically once
   > it's enabled.
   > [Open System Settings] [Cancel]
   The app polls status every second and proceeds automatically once `.enabled`.
3. macOS may ask for an administrator password on that toggle if the account is not an
   admin; Wayfork itself never prompts for a password.

## Notifications

`UserNotifications`, opt-out in General: tunnel entered `failed` (permanent), routing
engine crashed / could not start, helper lost approval. Never for transient reconnects.
The engine notification fires once per failure streak and says the app keeps retrying; the
retries themselves are silent (05-daemon.md, "Engine failure recovery").

## Error catalogue

| Code | When | User message | Recovery offered |
|------|------|--------------|------------------|
| `helper.notApproved` | register() → requiresApproval | see alert above | Open System Settings |
| `helper.versionMismatch` | handshake | "Updating helper…" (silent) | auto re-register; on failure: Reinstall Helper |
| `helper.unreachable` | XPC invalidated | "Can't reach the Wayfork helper." | Retry / Reinstall Helper |
| `singbox.configInvalid` | `sing-box check` failed | "Routing config rejected: <first line>". Should not happen with generated configs; include in diagnostics | Export Diagnostics |
| `singbox.startFailed` | startup verification fails twice / 3 crashes | "Routing engine failed to start. Another VPN may be active." | Show Log; the app re-applies on its own with backoff |
| `ovpn.authFailed` | `>PASSWORD:Verification Failed` | card: "failed: server rejected username/password" | ✎ → credentials |
| `ovpn.keyPassphrase` | private key decrypt failed | card: "failed: wrong key passphrase" | ✎ → passphrase |
| `ovpn.configError` | openvpn exits with options error | card: "failed: OpenVPN rejected the config" + line in Logs | ✎ → Replace Config |
| `ovpn.unreachable` | RECONNECTING loop > 2 min | card stays "reconnecting… attempt N · <reason>" | ↻ / Logs |
| `import.ovpn.missingFiles` | referenced files absent | "ca.crt, client.key referenced but not found next to the .ovpn" | pick folder / cancel |
| `import.ovpn.unsupported` | e.g. `dev tap` | "TAP configs are not supported." | — |
| `import.vless.invalid` | parse error | "Not a valid vless:// URL: <reason>" | — |
| `import.vless.unsupported` | e.g. `headerType=http`, transport `kcp` | "Transport \"kcp\" is not supported yet." | — |
| `import.link.invalid` | F13: parse error in `ss://`, `trojan://`, `vmess://` | "Not a valid <scheme> link: <reason>" | — |
| `import.link.unsupported` | F13: legacy SS cipher, SIP003 `plugin=`, VMess `aid > 0` or a non-V2RayN form, Trojan without TLS | "<what> is not supported: <why>" | — |
| `import.wireguard.invalid` | F13: conf without `PrivateKey`/`Address`, a peer without `Endpoint`/`AllowedIPs`, a key that is not 32 bytes | "Not a valid WireGuard config: <reason>" | — |
| `rule.invalid` | validation | inline: "Not a valid domain" / "`*` only allowed in wildcard rules" / "Not a valid IP address or subnet" / "This is an IP address — pick the IP match" / "This range is reserved" | — |
| `store.corrupt` | load | "Settings file was unreadable and has been reset. A backup is at …" | Reveal in Finder |

## Variant C — the F14–F18 wave (2026-09-15)

[prototype/variant-c.html](prototype/variant-c.html) (boards C1–C7) redraws the popover,
Settings › Tunnels, Rules and General with a first-time user in mind and adds the screens
of F14 (latency), F15 (Recent), F16 (groups), F17 (local proxy) and F18 (block lists).
The sections above stay the reference for everything they say and this section does
not; where the two disagree, this section wins. The friction audit that drove the
wording is the comment at the top of the prototype file. Milestones: M10 (this section's
wording and states on the existing features), M9, M11–M14 (one per feature).

### Wording (M10)

Strings are English, short and in the user's words. The table is the contract for M10;
`Strings.swift` gets one entry per row so the Windows client can copy them verbatim.

| Where | Was (variant B) | Is (variant C) |
|-------|-----------------|----------------|
| card / row, protocol | badge `OpenVPN` / `VLESS` in line 1 | protocol only in the Settings row subtitle (`· OpenVPN ·`); the popover card has no badge |
| card line 2, connected | `connected · <ip> on <utun> · N rules` | `Connected · ↓ … ↑ … · N sites` (`Idle` instead of zero rates) |
| card line 2, VLESS ready | `ready · <host> · N rules` | `Connected · Idle · N sites` — one word for one state |
| card line 2, connecting | `connecting… attempt N · <reason>` | `Connecting… · 2nd try` (ordinal, no reason code; the reason is in Logs) |
| card line 2, failed | `failed: server rejected username/password` | `Server refused the login` (see the error catalogue below) |
| card line 2, unreachable (F14) | — | `No answer through the tunnel for <duration>` |
| card, default tunnel | `· everything else` in line 2 | `Default` badge (accent) next to the name |
| summary, on with default | `On — everything via Work · 6 rules, 2 exceptions` | `On — everything goes via Work, N sites via other tunnels` |
| summary, on without default | `On — routing N domains through M tunnels` | `On — N sites via M tunnels, the rest as usual` |
| summary, off | `Off — all traffic goes direct` | `Off — nothing goes through a tunnel. Turn on to send your N sites through their tunnels; everything else stays as it is.` |
| summary, degraded | `Degraded — Lab is failing · …` | `Lab can't connect — 2 of 3 tunnels up, everything else via Work` |
| summary, default down | `Degraded — Work (default) is down · unmatched traffic is blocked` | `Work can't connect — sites without a rule are blocked until it is back` |
| Direct row / group | `Direct · exceptions` / `Direct` + badge `Exceptions` | `Not via any tunnel` (row: `· N sites`; group header hint: `stay on your normal connection, whatever other rules say`) |
| rule counts on cards, rows, groups | `N rules` | `N sites` (`1 site`); app rules count as sites too — the word is for the user, the page is still called Rules |
| match kind | `Suffix` / `Exact` / `Wildcard` / `App` / `IP` | `and subdomains` / `exactly this` / `pattern` / `the app` / `address range` |
| shadowed chip | `shadowed` + tooltip | the sentence in the row: `never used — "Not via any tunnel" has it` / `never used — Work has it` |
| group header `+` | icon button | `+ Add site` (menu: *Site* / *Application…* as before) |
| quick add placeholder | `Route domain…` | `Site to route, e.g. example.com` |
| reconnect | `↻` on every card | Retry `↻` only on a card that is failed, unreachable or reconnecting |
| failed tunnel action | `✎` | `Fix…` (opens the expanded row with the offending field focused) + Retry |
| expanded row: Username / Password | two rows | one row `Login` with both fields |
| expanded row: DNS Automatic | `Automatic (10.8.0.1)` | `From the tunnel (10.8.0.1)` |
| expanded row: default toggle | `Route everything else through this tunnel` | label `Everything else`, toggle text `Sites without a rule go through this tunnel` |
| expanded row: Config | `Aug 25, 2026 · ab12…9f3e` | `Imported Aug 25, 2026` (the hash stays in diagnostics) |
| General › Startup | `Launch Wayfork at login` / `Connect on launch` | `Open Wayfork at login` / `Turn on when it opens` |
| General › Reliability | `Reconnect tunnels automatically` / `Notify when a tunnel fails` | `Reconnect tunnels on their own` / `Notify me when a tunnel stops working` |
| General › DNS | `Direct traffic resolver: System / Custom` | `DNS for sites outside tunnels: Same as macOS / Custom`, hint `Sites that go through a tunnel always use that tunnel's DNS` |
| General › Logs | `Level: Info` | `Detail: Normal` (Error → *Errors only*, Warning → *Problems*, Info → *Normal*, Debug → *Everything*) |
| General › Helper | `Helper installed · v0.3.0` | `Helper installed · up to date` (`· needs update` / `· not installed`) |
| General › Backup | its own group | one `Backup…` button next to `Export Diagnostics…`, opening a small menu *Export…* / *Import…* |

Status words per tunnel state (the bold word that opens card line 2 and the Settings row
subtitle): `Connected`, `Connecting…`, `Reconnecting…`, `Not reachable` (F14, red),
`Can't connect` (failed, red, followed by the reason), `Not running` (global off),
`Off` (tunnel disabled, Settings only), `Idle` is never a state — it replaces the rates.

### Popover (C1, C2)

360 pt wide. Sections with 11 pt semibold headers: **Tunnels**, **Recent** (F15). Card
line 1: status dot, name, `Default` badge, then right-aligned the latency (F14) and the
sparkline; a Retry button only when there is something to retry. Line 2: the status
word and at most two facts. Group cards (F16) list their members under line 2. The
`Not via any tunnel` row follows the cards. Quick add and the footer as before, the
footer without the ⌘ hints' explanation (they stay).

- **Latency (F14)**: `62 ms` in 13 pt medium, unit in 10 pt, colour by band — green
  ≤ 100 ms, amber ≤ 300 ms, red above, tertiary grey `—` when no sample yet. The
  sparkline is 64 × 24 pt, the last 2 minutes (12 samples at the 10 s probe interval),
  the line in the band colour of the *current* value, a dot on the last point; a run of
  failed probes is drawn as a dashed baseline segment. Tooltip on the number: `Measured
  through the tunnel every 10 s · last 2 min: min 58, max 71 ms`. *Not reachable*: the
  number is replaced by the red words, line 2 says `No answer through the tunnel for
  <duration>` and the Retry button appears. Neither the number nor the sparkline is shown
  while the tunnel is not connected or the global state is off.
- **Group card (F16)**: square accent glyph instead of the status dot, `Group` badge, the
  active member's latency and sparkline on the right. Line 2: policy word (`Fastest` /
  `First live`) `· using <member> ·` the group's own rates `· N sites`. Member rows (11 pt,
  indented): dot, name, `✓ in use` in accent on the active one, `skipped — not reachable` /
  `skipped — off` on the others, latency right-aligned. A group with no usable member:
  red dot, line 2 `No member reachable · its N sites go via <default> for now` (or `… are
  blocked for now` when the group is the default exit).
- **Recent (F15)**: header `Recent — went via <default>, last 5 min` (`went direct` without
  a default tunnel) with the count `5 of 12` on the right; up to five rows, newest first:
  app icon (16 pt, from the process path via `NSWorkspace`, a generic terminal glyph for
  a CLI), the domain (12 pt, middle-truncated), the app name (11 pt secondary), a
  `Route via ▾` menu listing every tunnel and group except the default one, and `×` on
  hover (hide for the session). Choosing a tunnel creates a `suffix` rule for the
  registrable domain (`news.example.com` → `example.com`; the row shows what will be
  created in the menu's header: `Route example.com and subdomains via…`) and the row
  disappears. Empty state, in the card colour: clock glyph + `Sites you open from now on
  show up here — the ones that went via Work because no rule said otherwise. One click
  sends any of them through another tunnel.` The section is absent while off.
- **States (C2)**: *off* — the summary sentence from the table, cards dimmed with `Not
  running`, no numbers, quick add shown, Recent hidden. *Can't connect* — the summary
  names the tunnel, its card shows the reason and `Fix…` + Retry; a reconnecting tunnel
  shows the half dot and `Connecting… · 2nd try`. *No tunnels yet* — the toggle is
  disabled; the body is the fork glyph, `Add a VPN you already have`, one sentence (`An
  OpenVPN file (.ovpn), a VLESS or WireGuard link, or a subscription URL. Then tell
  Wayfork which sites go through it — the rest of your traffic is not touched.`), the
  primary `Add a tunnel…` (opens Settings › Tunnels › + Add) and `or drop a .ovpn file on
  this window`; quick add, the sections and the Direct row are hidden.

### Settings › Tunnels (C3, C7)

720 × 480 minimum (was 820 × 560), sidebar 150 pt. Row: dot, name, subtitle
`<Status> · <detail> · <Protocol> · N sites` (the default tunnel: `routes everything else
and N sites`), latency + sparkline right-aligned, enable toggle, chevron.

- **Expanded tunnel**: Name; Login (user + password fields on one row, OpenVPN only);
  DNS (`From the tunnel (<ip>)` / Custom); Everything else (toggle + sentence); **Local
  proxy** (F17); File (`Imported <date>`, Replace…) or Link (masked, Copy, Replace link…);
  footer `N sites · Show rules … Reconnect · Delete…`.
- **Local proxy row (F17)**: off — toggle + `Off` + hint `Turn on to get a 127.0.0.1
  address that sends an app through <name> without a rule — for curl, a browser profile,
  Telegram.` On — toggle + `127.0.0.1:1081` (monospace, port bold, editable on click) +
  Copy (copies `socks5h://127.0.0.1:1081`) + hint `for curl, a browser profile, Telegram
  — uses <name>, no rule needed`. Port validation inline: `Ports 1024–65535` / `Port
  already used by <other tunnel>`. A tunnel that is not connected keeps its row; the
  address is still shown (the port is listening whenever the engine runs; a flow through
  a down tunnel fails like any routed flow).
- **Group row and expanded group (F16)**: accent square glyph, `Group` in the subtitle
  (`Group · fastest of Home, Lab · using Home · N sites`), latency of the active member.
  Expanded: Name; **Pick by** segmented `Fastest` / `First live` with the one-line
  meaning next to it (`the member that answers quickest right now` / `the top member that
  works; the ones below are backups`); **Members** as ordered rows with a drag grip, dot,
  name, `✓ in use` / `skipped — not reachable` / `skipped — off`, latency, and `+ Add
  member…` (menu of tunnels not yet in the group); Local proxy (same row as a tunnel);
  footer `N sites · Show rules … Delete…`. Removing a member: the row's context menu or
  Delete key; the last two members cannot be removed — the group is deleted instead
  (`Delete group and its N rules?`).
- **New group…**: last row of the list, accent; a hint `— several tunnels behind one
  name, the fastest or the first live one is used` until the first group exists. Opens the
  **New group sheet (C7)**: Name (default `Group N`), Members (every tunnel with a tick
  and a grip; order by drag; latency per row), Pick by (two radios with the sentence each
  — *Fastest*: `Whichever member answers quickest right now — switches when another one
  gets faster.`; *First live*: `Always the top member that works; the ones below are
  backups, in order.`), Cancel / Create (enabled from two ticks on).
- The `+ Add` menu is unchanged; a group is not "added", it is created from the list.

### Settings › Rules (C4, C5)

- **Recent strip (F15)** above the groups: header `Recent · went via <default> in the
  last 5 min — pick a tunnel to make a rule` with `Show all N` on the right; three rows
  like the popover's; collapses to the header line `nothing new in the last 5 min` when
  empty. *Show all* expands the strip to every row (max 200) with a scroll. Hidden while
  off. Strip, not side column: at 720 pt a column squeezes the patterns and sits empty
  most of the day.
- **Group headers** say what they mean: `Not via any tunnel` + hint; `Work` + `Default`
  badge + `everything without a rule goes here, plus:`; a group section `Streaming` +
  `Group` badge + `fastest of Home, Lab · using Home right now`; a disabled tunnel
  `off — its sites go via <default> for now`; a not-reachable tunnel `not reachable — its
  sites wait` (red). `+ Add site` on the right of each header.
- **Rows**: checkbox, pattern (mono), match words from the table, note / warning sentence,
  grip. A disabled rule is greyed with `paused`. Empty group: `No sites yet — add one, or
  pick a tunnel for a site in Recent above.`
- **Rule target picker** (context menu *Move to…*, and the quick-add popup): tunnels, then
  groups under a separator, then *Not via any tunnel*.
- **Tester line** (07-rule-testing.md) reads `Where does [ site ] go?  [Test] [~ Probe]`;
  results per that document, the exit in the user's words (`Streaming (using Home right
  now)`, `Not via any tunnel`, `Stays local`); `N notes ▾` instead of `caveats`. The
  **Probe** result is a chip on a line under the result: `Reachable · 212 ms via Home`
  (green), `Blocked · on your ads & trackers list · Never block this site` (red; F18),
  `Failed · Lab is not reachable right now · Retry Lab`, `Failed · no answer from the site
  in 10 s — the tunnel itself is fine (62 ms)`; while running, a spinner with `Probing
  <host> via <tunnel>… up to 10 s`. Details in 07-rule-testing.md § Probe.

### Settings › General (C6)

Section order: **Blocking** (F18), Startup, Reliability, DNS, Logs, Helper & About.

- **Block ads and trackers**: toggle; hint `Blocked N today · list of N sites · from
  Wayfork <version>` (the first cut ships the list with the app; when a refresh job
  exists the hint gains `updated <age> · Update now` as drawn on C6). `Blocked N today`
  counts the flows and lookups the list rejected since local midnight, kept by the daemon
  in memory (05-daemon.md); while the log detail is above *Normal* the counter reads
  `Blocked — (counting needs log detail Normal)`.
- **Never block**: chips with `×`, an `Add a site…` field with the domain validation of
  the rule editor (suffix semantics, so `example.com` also frees `cdn.example.com`).
  Hint `Sites the list gets wrong — they load as usual`.
- The other rows per the wording table.

### Notifications

Unchanged set; the F14 *unreachable* state does **not** notify (a candidate in
next-features.md, after F14 ships).

### Error catalogue additions

| Code | When | User message | Recovery offered |
|------|------|--------------|------------------|
| `ovpn.authFailed` (reworded) | as before | card: `Server refused the login` | Fix… → credentials |
| `ovpn.keyPassphrase` (reworded) | as before | card: `Wrong key passphrase` | Fix… → passphrase |
| `ovpn.configError` (reworded) | as before | card: `OpenVPN rejected the file` | Fix… → Replace… |
| `ovpn.unreachable` (reworded) | as before | card: `Connecting… · Nth try` | Retry / Logs |
| `probe.unreachable` | F14: N probes failed in a row | card: `Not reachable` + `No answer through the tunnel for <duration>` | Retry |
| `group.noMember` | F16: no usable member | group card: `No member reachable · its N sites go via <default> for now` | — |
| `group.invalid` | F16: fewer than two members, a member that is a group, unknown member | inline in the sheet: `Pick at least two tunnels` / (others cannot happen from the UI; on import the group is skipped with a warning) | — |
| `proxy.portInUse` | F17: bind failed at engine start | row: `Port 1081 is taken by another program — pick another` (engine starts without that inbound) | edit port |
| `proxy.portInvalid` | F17: validation | inline: `Ports 1024–65535` / `Port already used by <tunnel>` | — |
| `blocklist.missing` | F18: bundled list not found next to the binary | toggle disabled, hint `The block list is missing from this build — reinstall Wayfork` | Reinstall |
